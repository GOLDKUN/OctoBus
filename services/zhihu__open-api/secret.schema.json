{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "accessSecret": {
      "type": "string",
      "minLength": 1,
      "description": "Zhihu Open Platform Access Secret, sent as the Bearer token. Created in the platform personal center."
    },
    "access_secret": {
      "type": "string",
      "minLength": 1,
      "description": "Deprecated alias for accessSecret."
    },
    "oauthToken": {
      "type": "string",
      "minLength": 1,
      "description": "Optional Zhihu OAuth access token. Sent as X-OAuth-Token to query an authorized user's data; omit to query the Access Secret owner."
    },
    "oauth_token": {
      "type": "string",
      "minLength": 1,
      "description": "Deprecated alias for oauthToken."
    }
  }
}
