{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "apiKey": {
      "type": "string",
      "minLength": 1,
      "writeOnly": true,
      "description": "Cortex API key (preferred auth method, passed as Bearer token)."
    },
    "username": {
      "type": "string",
      "minLength": 1,
      "writeOnly": true,
      "description": "Cortex username for Basic Auth (fallback when apiKey is not set)."
    },
    "password": {
      "type": "string",
      "minLength": 1,
      "writeOnly": true,
      "description": "Cortex password for Basic Auth (fallback when apiKey is not set)."
    }
  }
}
